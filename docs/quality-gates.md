# Quality Gates（目标值）

这些是首版待验证的产品目标，不是当前已达成的线上指标：

- 强拍切点中位误差 ≤ 40ms。
- 候选充足时，相邻镜头不重复配色；颜色配额与理想值最多相差 1。
- 用户指定动作全部覆盖；动作转场人工判定有效率 ≥ 90%。
- 可用镜头比例 ≥ 95%，输出无明显音画漂移。
- 相同输入 manifest、配置和模型响应快照可复现同一 Edit Plan。

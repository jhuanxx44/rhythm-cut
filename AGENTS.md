# Rhythm Cut — Agent 工作规约

## 开始工作前

1. 阅读 `README.md`、相关 `docs/`、`task_plan.md` 和近期 `progress.md`。
2. 检查 `git status`，识别已有未提交改动，不覆盖或回滚他人的工作。
3. 说明本次改动的目标、影响范围和验证方式。
4. 如果需求与已有契约冲突，记录冲突并优先保留可验证的契约。

## 工程边界

- `src/rhythm_cut/domain/`：稳定的数据模型、校验和领域规则；不得依赖具体外部 SDK。
- `src/rhythm_cut/analysis/`：音频、视频和视觉分析实现。
- `src/rhythm_cut/providers/`：外部模型或服务适配器；供应商细节只能停留在这里。
- `src/rhythm_cut/rendering/`：预览、导出和渲染实现。
- `prompts/`：版本化提示词和结构化输出约定。
- `configs/`：可审查的默认配置；秘密只能来自环境变量或本地未提交文件。
- `docs/`：架构、接口、决策和验证标准。
- `tests/`：离线单元测试、契约测试和固定 fixture。
- `local/`：本地素材、缓存和临时产物，不提交到 Git。

## 编码规则

- Python 3.11+，公共函数使用类型标注和 docstring。
- 时间值必须明确单位；媒体计算同时保留秒和帧的语义，最终渲染以帧为准。
- 外部服务调用必须有明确的接口、超时、错误处理和可离线替代的 fixture。
- 不在 Agent 输出中执行未经校验的任意 shell、Python 或媒体命令；命令由受控模块生成。
- 不硬编码 API key、用户路径、素材 URL 或机器相关配置。
- 改动应保持小而可回滚；不要为了“顺便整理”扩大范围。

## 证据与状态

区分以下状态：代码已实现、测试通过、本地媒体渲染通过、部署运行、产品质量达标。模型输出、日志中的 `success` 或 HTTP 200 都不能单独证明后几项。

涉及模型或媒体分析时，尽量保存：输入文件哈希、模型和版本、提示词版本、采样参数、时间窗口、原始响应、归一化结果和错误信息。无法确认的内容标记为待验证。

## 验证要求

完成改动后至少运行与影响范围匹配的检查，例如：

```bash
.venv311/bin/pytest
.venv311/bin/ruff check src tests
```

如果涉及渲染、模型调用或真实素材，单独报告哪些检查已完成、哪些仍是 fixture 或离线验证。不要把未执行的检查写成通过。

## 文档与规划

- 长期规则写在本文件。
- 当前产品假设、阶段路线、实验结果写在 `task_plan.md`、`findings.md`、`docs/` 或 `progress.md`。
- 修改接口、数据模型、提示词或外部 provider 时，必须同步更新对应文档和测试。
- 任务结束时记录改动文件、验证命令、失败与遗留风险。
